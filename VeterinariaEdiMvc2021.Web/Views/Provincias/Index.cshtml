@model PagedList.IPagedList<VeterinariaEdiMvc2021.Entidades.ViewModels.Provincia.ProvinciaCantidadListViewModel>
@using PagedList.Mvc
@{ ViewBag.Title = "Index"; }
<div>
    <a href="@Url.Action("Create","Provincias")" class="btn btn-primary" style="float: right">Nuevo</a>
    <h2>Lista de Provincias</h2>

</div>
<hr />
@if (TempData["Msg"] != null)
{
    var msg = TempData["Msg"].ToString();
    <div class="alert alert-success alert-dismissible">
        <button type="button" class="close" data-dismiss="alert">&times;</button>
        <strong>Éxito!</strong> @msg
    </div>}
<table class="table table-bordered table-hover">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.First().NombreProvincia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.First().CantidadProvincias)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreProvincia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CantidadProvincias)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.ProvinciaId }, new { @class = "btn btn-warning" })
                @Html.ActionLink("Borrar", "Delete", new { id = item.ProvinciaId }, new { @class = "btn btn-danger" })
            </td>
        </tr>
    }




</table>
</>
</>
Pàgina:  @Model.PageNumber  de @Model.PageCount
@Html.PagedListPager(Model, page => Url.Action("Index", new
{
    page
}))
